/**
 * Admin Dashboard (`/admin`) — per architecture.md §5.3.
 * Layout reference: Dify Image 4 sidebar pattern (do NOT copy code per CLAUDE.md §7).
 */

export default function AdminDashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">EKP — Admin Dashboard</h1>
      <p className="mt-2 text-muted-foreground">
        W1 skeleton. KB stats + ingestion log + quick actions (W2 deliverable per §5.3).
      </p>
    </main>
  );
}

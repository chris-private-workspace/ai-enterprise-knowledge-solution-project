/**
 * KB List (`/admin/kb`) — per architecture.md §5.4.
 * Card grid + "Create KB" button. W2 deliverable.
 */

export default function KbListPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Knowledge Bases</h1>
      <p className="mt-2 text-muted-foreground">
        W1 skeleton. Card grid + create button (W2 per §5.4).
      </p>
    </main>
  );
}

/**
 * End User Chat (`/`) — per architecture.md §5.2.
 * W1 skeleton placeholder. W3 wires Vercel AI SDK `useChat` against /query/stream SSE.
 */

export default function ChatPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-semibold">EKP — End User Chat</h1>
      <p className="mt-4 text-muted-foreground">
        W1 skeleton. Streaming chat impl per architecture.md §5.2 (W3 deliverable).
      </p>
    </main>
  );
}

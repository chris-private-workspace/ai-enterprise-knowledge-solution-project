/**
 * Eval Console (`/eval`) — per architecture.md §5.6.
 * 4-metric cards + W4 reranker shootout table. W4 deliverable.
 */

export default function EvalConsolePage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Evaluation Console</h1>
      <p className="mt-2 text-muted-foreground">
        W1 skeleton. 4-metric cards + reranker shootout (W4 per §5.6).
      </p>
    </main>
  );
}
